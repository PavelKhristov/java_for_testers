package manager;

import org.openqa.selenium.By;

public class BaseHelper {
    /*public final ApplicationManager manager;
    public GroupHelper (ApplicationManager manager){
        this.manager = manager;

    private GroupHelper groups;
    public GroupHelper groups () {
        if (groups == null){
            groups = new GroupHelper(this);
        }
        return groups;
    }*/



    /*public final GroupHelper groupmanager;
    public BaseHelper(GroupHelper groupmanager) {
        this.groupmanager = groupmanager;
    }



    public void ClickButton(String button) {
        groupmanager.manager.driver.findElement(By.name(button)).click();
    }

    void ClickLink(String link) {
        groupmanager.manager.driver.findElement(By.linkText(link)).click();
    }

    void FillInTheFieldByValue(String fieldName, String group) {
        groupmanager.manager.driver.findElement(By.name(fieldName)).sendKeys(group);
    }*/

}
